#include<stdio.h>
#include<stdlib.h>

int main(int argc, char const *argv[])
{
    /* code */
    int  *ptr;
    ptr = (int*)malloc(sizeof(int));
    if (ptr == NULL)
    {
        /* code */printf("分配动态内存失败");
        return 0;
    }
    printf("qingshuru");
    scanf;    
    return 0;
}
